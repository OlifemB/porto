import { atom } from 'recoil'

export const parallaxCurrentState = atom({
    key: 'parallaxCurrentState',
    default: 0,
})

export const mainPageState = atom({
    key: 'mainPageState',
    default: null,
})
